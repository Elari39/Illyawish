# 本地 Docker 运行说明已迁移

本地 Docker 运行说明已经并入新的中文主文档：

- 中文完整说明：[`README.zh-CN.md`](README.zh-CN.md)

对应章节：

- `本地或服务器直接用 Docker Compose 启动`
- `更新、回滚与排错`

核心约定不变：

- 本地访问入口仍是 `http://localhost:10170`
- 后端仍只在内部使用 `5721`
- 持久化数据仍位于项目根目录 `data/`
